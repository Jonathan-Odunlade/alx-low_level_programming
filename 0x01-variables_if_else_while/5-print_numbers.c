#include <stdio.h>
/**
 *main - this prints numbers from 0 to 9
 *
 *Return: Always 0 when code is successful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
