#include <stdio.h>

/**
 * main - this prints numbers from o and 9
 *
 * Return : always 0 (when code is successful)
 */


int main(void)

{

    int c;

    c = 0;
    while 
	   (c < 10 ) {
		   putchar(c + '0');
		   c++;
	   }
    putchar('\n');

    return (0);
}
