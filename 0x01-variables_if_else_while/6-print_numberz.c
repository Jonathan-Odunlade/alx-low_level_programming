#include <stdio.h>
/**
 * main - this is the entry point of the code
 *
 * Return: 0 (when code is successful)
 */

int main(void)
{
	int c;

	for (c = '0'; c <= '9'; c++)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
