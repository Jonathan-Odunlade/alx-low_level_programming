#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers
 * @a: one of the chars to be multiplied
 * @b: the second char
 * Return: 0
 */

int mul(int a, int b)
{
	return (a * b);
}
